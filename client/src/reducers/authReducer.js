const initialState = {
  isAuthenticated: false,
  user: {}
};

const autReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default autReducer;
